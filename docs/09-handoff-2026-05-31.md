# Handoff - 2026-05-31

## Current State

This cleanup pass focused on making runtime ownership explicit and reducing duplicate paths.

Canonical roots:

- User/business runtime data: `persona/`
- System observability logs: `monitor/`
- Source-like mode config: `mode/`
- Source-like subagent prompts/processors: `subagent/`

## Completed

- Moved active runtime references to `persona/`:
  - session data: `persona/sessions/`
  - global event stream: `persona/events/thread.jsonl`
  - memory: `persona/memory/MEMORY.md`
  - cron/count trigger state: `persona/trigger/`
- Kept monitor logs at root:
  - `monitor/subagent_runs.jsonl`
  - `monitor/trigger_decisions.jsonl`
- Added trigger decision observability:
  - records skipped/no_delta/eligible/spawned/failed decisions
  - renders in WebUI monitor
- Added local cost estimate panel in monitor based on logged token usage.
- Unified Benative runtime paths under `persona/benative/`.
- Removed old KG module: `subagent/cross_session/kg/`.
- Removed unused empty `subagent/_cursors` and placeholder runtime directories.
- Rewrote `architecture.md` for the current implementation.
- Marked `WikiUpdater` as legacy explicit patch JSONL importer; current wiki sync uses `persona/events/thread.jsonl`.

## Validation Already Run

- `uv run python scripts/validate_subagent_config.py`
- `uv run python -m py_compile bot/nanobot/command/builtin.py subagent/cross_session/wiki/processor/wiki_updater.py`
- `uv run python -m pytest bot/tests/command/test_benative_command.py bot/tests/wiki/test_wiki_updater.py`
- Earlier in the same pass:
  - backend trigger/admin/subagent tests
  - `pnpm run check` in `bot/webui`

## Still Worth Doing

- Run full backend test suite when budget/time allows:
  - `uv run python -m pytest bot/tests`
- Run WebUI check again after any frontend follow-up:
  - `pnpm run check` in `bot/webui`
- Start the gateway and visually verify monitor:
  - `uv run nanobot gateway`
  - open WebUI monitor
- Monitor log retention/rotation has a utility and tests; keep runtime log files ignored.
- Old planning docs were moved to `docs/deprecated/` and should be treated as historical material only.
- Keep `config/capabilities.yaml` updated before adding modes, triggers, subagents, processors, or monitor streams.

## Notes For Next Agent

- Do not reintroduce `shared/benative` or root `data/`, `sessions/`, `memory`, `trigger`.
- New runtime files under `persona/` and `monitor/` are mostly ignored by git.
- If a trigger appears not to fire, inspect `monitor/trigger_decisions.jsonl` first, then `monitor/subagent_runs.jsonl`.
- KG has intentionally been removed; LLM Wiki is the active knowledge system.
