{
  "version": 1,
  "triggers": [
    {
      "id": "memory_cron",
      "name": "Memory Cron",
      "enabled": true,
      "condition": {
        "kind": "cron",
        "count": "0 0 * * *",
        "scope": "global"
      },
      "target": {
        "subagent": "memory_cron",
        "prompt_file": "subagent/cross_session/memory_cron/context/memory_cron_subagent.md",
        "model": "deepseek-v4-flash",
        "task_template": "Update user memory profile from incremental session deltas only.\n\nSession deltas (JSON):\n{modified_sessions}\n\nEach entry contains only new thread.jsonl records since the previous successful memory_cron run. Extract NEW facts/preferences from new_messages and merge into {workspace}/persona/memory/MEMORY.md.\n\nDo not re-read whole thread.jsonl files unless the delta explicitly indicates a reset."
      },
      "cursor": {
        "last_fired": null,
        "last_checked": null
      }
    },
    {
      "id": "daily_consolidator",
      "name": "Daily Consolidator",
      "enabled": true,
      "condition": {
        "kind": "cron",
        "count": "0 */8 * * *",
        "scope": "global"
      },
      "target": {
        "subagent": "daily_consolidator",
        "prompt_file": "subagent/cross_session/daily_consolidator/context/daily_consolidator_subagent.md",
        "model": "deepseek-v4-flash",
        "task_template": "Aggregate incremental vocab.md and polisher.md deltas from sessions modified since last run.\n\nNote deltas (JSON):\n{modified_sessions}\n\nEach entry contains vocab_delta and/or polisher_delta text appended since the previous successful daily_consolidator run. Process only those deltas.\n\nWrite to: {workspace}/subagent/cross_session/daily_consolidator/data/daily/daily_{date}.md"
      },
      "cursor": {
        "last_fired": null,
        "last_checked": null
      }
    }
  ]
}
